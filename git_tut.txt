1. command {{ ssh-keygen }} liq@ enter

2. gnum es qo user-i papken @nde .ssh papka ka mtnum es pti id_rsa.pub file lini et faili 
	parunakutyun@ copy es anum avelacnum es qo git-i accaunt-i settingsneri mej ssh klyuchi tex ka. @tex.
3. gnum es htdoc copy es anum SSH clone URL cod@ qo repo-i mejic u bacum es terminal@ grum es git clone u et paste es anum .


htdocsum git clone git@github.com:lemar5555/MyTuts.git

composer install

virtual host ksarges vor ashxati


git add --all

git commit -m "koment"

git pull origin master

git push origin master 